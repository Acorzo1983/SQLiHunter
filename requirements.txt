# Core HTTP and async libraries
aiohttp>=3.8.0
requests>=2.28.0
# Terminal colors and formatting
colorama>=0.4.4
# HTML parsing (legacy compatibility)
beautifulsoup4>=4.11.0
# URL parsing and validation
urllib3>=1.26.0
# Optional but recommended: Progress tracking
tqdm>=4.64.0
# Optional: Enhanced console output
rich>=12.0.0
# Optional: Faster JSON parsing
ujson>=5.4.0
